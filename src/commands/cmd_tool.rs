//! Port of src/commands/cmd_tool.cpp. Owner: the run/llm/tool/serve port.
use crate::cli::App;

pub fn register_tool(app: &mut App) {
    let _ = app;
    todo!("run/llm/tool/serve port: register_tool")
}

pub fn configure_tool_call(cmd: &mut App) {
    let _ = cmd;
    todo!("run/llm/tool/serve port: configure_tool_call")
}
