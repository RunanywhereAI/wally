//! Port of src/commands/cmd_stt.cpp. Owner: the dormant audio modalities port.
use crate::cli::App;

pub fn register_stt(app: &mut App) {
    let _ = app;
    todo!("dormant audio modalities port: register_stt")
}
