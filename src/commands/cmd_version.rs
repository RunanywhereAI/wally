//! Port of src/commands/cmd_version.cpp. Owner: the maintenance/diagnostics port.
use crate::cli::App;

pub fn register_version(app: &mut App) {
    let _ = app;
    todo!("maintenance/diagnostics port: register_version")
}
