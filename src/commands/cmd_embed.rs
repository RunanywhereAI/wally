//! Port of src/commands/cmd_embed.cpp. Owner: the dormant vision/text modalities port.
use crate::cli::App;

pub fn register_embed(app: &mut App) {
    let _ = app;
    todo!("dormant vision/text modalities port: register_embed")
}
