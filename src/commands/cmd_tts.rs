//! Port of src/commands/cmd_tts.cpp. Owner: the dormant audio modalities port.
use crate::cli::App;

pub fn register_tts(app: &mut App) {
    let _ = app;
    todo!("dormant audio modalities port: register_tts")
}
