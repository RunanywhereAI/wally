//! Port of src/commands/cmd_bench.cpp. Owner: the maintenance/diagnostics port.
use crate::cli::App;

pub fn register_bench(app: &mut App) {
    let _ = app;
    todo!("maintenance/diagnostics port: register_bench")
}
