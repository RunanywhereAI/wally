//! Port of src/commands/cmd_telemetry.cpp. Owner: the maintenance/diagnostics port.
use crate::cli::App;

pub fn register_telemetry(app: &mut App) {
    let _ = app;
    todo!("maintenance/diagnostics port: register_telemetry")
}
