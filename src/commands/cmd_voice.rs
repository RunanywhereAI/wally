//! Port of src/commands/cmd_voice.cpp. Owner: the dormant audio modalities port.
use crate::cli::App;

pub fn register_voice(app: &mut App) {
    let _ = app;
    todo!("dormant audio modalities port: register_voice")
}
