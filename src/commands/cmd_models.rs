//! Port of src/commands/cmd_models.cpp. Owner: the models port.
use crate::cli::App;

pub fn register_models(app: &mut App) {
    let _ = app;
    todo!("models port: register_models")
}

pub fn register_models_aliases(app: &mut App) {
    let _ = app;
    todo!("models port: register_models_aliases")
}
