//! Port of src/commands/cmd_vad.cpp. Owner: the dormant audio modalities port.
use crate::cli::App;

pub fn register_vad(app: &mut App) {
    let _ = app;
    todo!("dormant audio modalities port: register_vad")
}
