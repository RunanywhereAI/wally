//! Port of src/commands/cmd_segment.cpp. Owner: the dormant vision/text modalities port.
use crate::cli::App;

pub fn register_segment(app: &mut App) {
    let _ = app;
    todo!("dormant vision/text modalities port: register_segment")
}
