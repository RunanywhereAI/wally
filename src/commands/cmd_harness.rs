//! Port of src/commands/cmd_harness.cpp. Owner: the coding-tool harness port.
use crate::cli::App;

pub fn register_harness(app: &mut App) {
    let _ = app;
    todo!("coding-tool harness port: register_harness")
}
