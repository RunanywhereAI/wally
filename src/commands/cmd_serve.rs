//! Port of src/commands/cmd_serve.cpp. Owner: the run/llm/tool/serve port.
use crate::cli::App;

pub fn register_serve(app: &mut App) {
    let _ = app;
    todo!("run/llm/tool/serve port: register_serve")
}
