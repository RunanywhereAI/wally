//! Port of src/commands/cmd_rm.cpp. Owner: the models port.
use crate::cli::App;

pub fn configure_models_delete(cmd: &mut App) {
    let _ = cmd;
    todo!("models port: configure_models_delete")
}
