//! Port of src/commands/cmd_info.cpp. Owner: the maintenance/diagnostics port.
use crate::cli::App;

pub fn register_info(app: &mut App) {
    let _ = app;
    todo!("maintenance/diagnostics port: register_info")
}
