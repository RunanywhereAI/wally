//! Port of src/commands/cmd_diarize.cpp. Owner: the dormant audio modalities port.
use crate::cli::App;

pub fn register_diarize(app: &mut App) {
    let _ = app;
    todo!("dormant audio modalities port: register_diarize")
}
