//! Port of src/commands/cmd_about.cpp. Owner: the maintenance/diagnostics port.
use crate::cli::App;

pub fn register_about(app: &mut App) {
    let _ = app;
    todo!("maintenance/diagnostics port: register_about")
}
