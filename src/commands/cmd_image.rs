//! Port of src/commands/cmd_image.cpp. Owner: the dormant vision/text modalities port.
use crate::cli::App;

pub fn register_image(app: &mut App) {
    let _ = app;
    todo!("dormant vision/text modalities port: register_image")
}
