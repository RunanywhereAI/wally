//! Port of src/commands/cmd_editors.cpp. Owner: the coding-tool harness port.
use crate::cli::App;

pub fn register_editors(app: &mut App) {
    let _ = app;
    todo!("coding-tool harness port: register_editors")
}
