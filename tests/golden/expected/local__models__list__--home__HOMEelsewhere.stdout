Download a model with `wally models pull <id>`:
  wally models pull qwen3-0.6b        # llama.cpp
  wally models pull mlx-qwen3-0.6b    # MLX (Apple GPU)

no models downloaded — try `wally models list --all` then `wally models pull <id>`
